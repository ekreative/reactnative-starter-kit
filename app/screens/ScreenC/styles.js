import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  wrapper: {
    flex: 1,
    alignItems: 'center',
    padding: 30
  },
  title: {
    fontSize: 16,
    fontWeight: 'bold'
  }
})
